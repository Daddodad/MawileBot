[SableyeBot]
bot_token = <bot token>                             # As pure values (no " ... ")
admin_whitelist = [ <chatid>, <chat id> ...]        # Access to admin commands 

[MawileBot]
bot_token = <bot token>                             # As pure values (no " ... ")
admin_whitelist = [ <chatid>, <chat id> ...]        # Access to admin commands   

[BeatlesBoy]
api_id = <api_id>                                   # As number
api_hash = <api_hash>                               # As pure values (no " ... ")
allowed_chat_ids =  [ <chatid>, <chat id> ...]      # So that BeatlesBoy does not answer in any chat but only designated groups/chats.

